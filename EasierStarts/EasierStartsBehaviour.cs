using UnityEngine;

namespace EasierStarts;

// Lightweight per-frame driver. (Re)created by Plugin.OnSceneLoaded on every scene load.
internal class EasierStartsBehaviour : MonoBehaviour
{
    private void Update()
    {
        // DefibroGranter.Tick() is wired in here in Task 4.
    }
}
